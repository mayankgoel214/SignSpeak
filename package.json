{
  "name": "signspeak",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8099 --directory web",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
